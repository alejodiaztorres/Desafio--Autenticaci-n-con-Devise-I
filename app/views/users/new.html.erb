<h1> Usuario nuevo </h1>
<p> <%= @user %> </p>

<%= form_with(model: @user, local: true) do |form| %>
  <div class="field">
    <%= form.text_area :name %>
    <%= form.text_area :name %>
  </div>

   <div class="field">
    <%= form.label :email %>
    <%= form.email_field :email %>
  </div>

   <div class="field">
    <%= form.label :password %>
    <%= form.password_field :pasword %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
